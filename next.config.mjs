/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["image.tmdb.org", "dummyimage.com", "img.youtube.com"],
    // Add more domains if needed
  },
};

export default nextConfig;
